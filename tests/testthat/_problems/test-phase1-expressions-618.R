# Extracted from test-phase1-expressions.R:618

# test -------------------------------------------------------------------------
x <- Variable(3, integer = TRUE)
val <- matrix(c(1.3, 2.7, -0.8))
result <- project(x, val)
