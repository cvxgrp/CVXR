% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/252_transforms_partial_optimize.R
\name{partial_optimize}
\alias{partial_optimize}
\title{Partial optimization transform}
\usage{
partial_optimize(
  prob,
  opt_vars = NULL,
  dont_opt_vars = NULL,
  solver = NULL,
  ...
)
}
\arguments{
\item{prob}{A \link{Problem} to partially optimise.}

\item{opt_vars}{Optional list of \link{Variable}s to optimise over.}

\item{dont_opt_vars}{Optional list of \link{Variable}s to keep as free
arguments of the resulting expression.}

\item{solver}{Optional solver name (passed to \code{\link[=psolve]{psolve()}} when the
PartialProblem is evaluated via \code{\link[=value]{value()}} or \code{\link[=grad]{grad()}}).}

\item{...}{Additional named arguments forwarded to \code{\link[=psolve]{psolve()}} when
\code{value()} / \code{grad()} are called.}
}
\value{
A \code{PartialProblem} expression.
}
\description{
Builds an Expression representing the optimal value of \code{prob} as
a function of the variables you choose NOT to optimise over.  Useful
for two-stage / hierarchical optimisation, custom atom definitions,
and embedding sub-problems inside larger problems.
}
\details{
Exactly one of \code{opt_vars} or \code{dont_opt_vars} may be \code{NULL}; the
missing list is taken to be the complement (relative to the full
list of variables in \code{prob}).  If both are supplied, they must
together cover every variable in \code{prob}.

The returned \code{PartialProblem} is an Expression with scalar shape:
it is convex when \code{prob} is DCP with a \code{Minimize} objective and
concave when DCP with \code{Maximize}.  Embed it like any other
expression in a larger Problem; the larger problem's canonicalizer
will pull the inner objective and constraints into the outer cone
form so a single solve handles both layers.
}
\examples{
\dontrun{
x <- Variable(3)
t <- Variable(3)
abs_x <- partial_optimize(
  Problem(Minimize(sum_entries(t)), list(-t <= x, x <= t)),
  opt_vars = list(t)
)
## abs_x is now an expression of x alone, equivalent to sum(abs(x)).
}

}
\seealso{
\link{Problem}, \code{\link[=psolve]{psolve()}}
}
