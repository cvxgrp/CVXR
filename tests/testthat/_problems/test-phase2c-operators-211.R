# Extracted from test-phase2c-operators.R:211

# test -------------------------------------------------------------------------
x <- Variable(3)
y <- Variable(3)
z <- Variable(3)
inner <- AddExpression(list(x, y))
