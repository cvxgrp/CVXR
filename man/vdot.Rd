% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/045_atoms_affine_binary_operators.R
\name{vdot}
\alias{vdot}
\title{Vector dot product (inner product)}
\usage{
vdot(x, y)
}
\arguments{
\item{x}{An Expression, numeric value, or nested list thereof. The
conjugate-linear argument.}

\item{y}{An Expression, numeric value, or nested list thereof. The linear
argument.}
}
\value{
A scalar Expression representing \code{sum(Conj(x) * y)}.
}
\description{
The standard inner product of \code{x} and \code{y}: both are flattened, multiplied
elementwise, and summed. \strong{Conjugate-linear in \code{x}} — \code{x} is conjugated
before multiplying, matching \code{numpy.vdot()} and CVXPY's \code{vdot()}. For real
arguments conjugation is the identity, so this is the ordinary dot product.

Either argument may also be a (possibly nested) list, which is flattened
before the product; the two are flattened independently, so their nesting
need not match.
}
\examples{
x <- Variable(3)
vdot(x, c(1, 2, 3))

a <- Variable(); b <- Variable()
vdot(list(a, b), c(1, 2))
}
\seealso{
\code{\link[=scalar_product]{scalar_product()}}, \code{\link[=cvxr_outer]{cvxr_outer()}}, \code{\link[=deep_flatten]{deep_flatten()}}
}
