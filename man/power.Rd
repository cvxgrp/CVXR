% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/064_atoms_elementwise_power.R
\name{power}
\alias{power}
\title{Create a Power atom}
\usage{
power(x, p, max_denom = 1024L, approx = TRUE)
}
\arguments{
\item{x}{An Expression (the base), OR a positive constant if \code{p} is a
variable (the identity \code{b^x = exp(x * log(b))} is used).}

\item{p}{Numeric exponent, Parameter, or Expression. If \code{p} is a
non-constant Expression and \code{x} is a positive constant, dispatches to
\code{exp(p * log(x))}.}

\item{max_denom}{Maximum denominator for rational approximation}

\item{approx}{If TRUE (default), use SOC approximation. If FALSE, use exact power cone.}
}
\value{
A Power or PowerApprox atom, or an exp expression for the const-base case.
}
\description{
Create a Power atom
}
\note{
\code{sqrt(x)} on a CVXR expression dispatches to
\code{Power(x, 0.5)} via the Math group generic.
See \code{\link{math_atoms}} for all standard R function dispatch.
}
