# Extracted from test-phase2c-operators.R:203

# test -------------------------------------------------------------------------
x <- Variable(c(1, 3))
y <- Variable(c(3, 3))
a <- AddExpression(list(x, y))
