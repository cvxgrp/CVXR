% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/049_atoms_affine_reshape.R
\name{deep_flatten}
\alias{deep_flatten}
\title{Recursively flatten a nested list of expressions into one column vector}
\usage{
deep_flatten(x)
}
\arguments{
\item{x}{An Expression, a numeric value, or a (possibly nested) list of them.}
}
\value{
An Expression of shape \code{c(n, 1)}.
}
\description{
Flattens \code{x} into a single column vector. An Expression or numeric is
vectorized column-major; a list is flattened element by element and the
pieces stacked in order, recursively. This is what lets \code{\link[=vdot]{vdot()}} accept
nested lists: \code{vdot(list(a, b), c(1, 2))} is \code{a * 1 + b * 2}.
}
\examples{
a <- Variable(); b <- Variable()
deep_flatten(list(a, b))
}
\seealso{
\code{\link[=vec]{vec()}}, \code{\link[=vdot]{vdot()}}
}
