% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/004_zzz_R_specific_generics.R,
%   R/253_reductions_solvers_solving_chain.R
\name{solve_via_data}
\alias{solve_via_data}
\title{Solve via Raw Data}
\usage{
solve_via_data(
  x,
  data,
  warm_start = FALSE,
  verbose = FALSE,
  solver_opts = list(),
  ...
)
}
\arguments{
\item{x}{A \code{SolvingChain} (preferred) or \code{Solver} object.}

\item{data}{Named list of solver data from \code{\link{problem_data}()}.}

\item{warm_start}{Logical; use warm-start if supported.}

\item{verbose}{Logical; print solver output.}

\item{solver_opts}{Named list of solver-specific options.}

\item{...}{Additional arguments (e.g., \code{problem} for
\code{SolvingChain} dispatch, \code{solver_cache} for
\code{Solver} dispatch).}
}
\value{
Solver-specific result (a named list).
}
\description{
Calls the solver on pre-compiled problem data (step 2 of the
decomposed solve pipeline). Dispatches on \code{x}: when \code{x}
is a \code{SolvingChain}, delegates to the terminal solver with
proper cache management.
}
\seealso{
\code{\link{problem_data}}, \code{\link{problem_unpack_results}}
}
