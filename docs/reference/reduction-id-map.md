# Reduction leaf-id maps

Reduction leaf-id maps

## Usage

``` r
var_id_map(x)

param_id_map(x)
```

## Arguments

- x:

  A `Reduction`.

## Value

A named list `orig-id -> character vector of reduced-id(s)`; empty by
default (the reduction replaces no leaves).
