# Extracted from test-phase2c-operators.R:101

# test -------------------------------------------------------------------------
x <- Variable(c(3, 1))
y <- Variable(c(3, 1))
result <- broadcast_args(x, y)
