% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/077_atoms_elementwise_logic.R
\name{And}
\alias{And}
\title{Logical AND}
\usage{
And(..., id = NULL)
}
\arguments{
\item{...}{Two or more boolean \link{Variable}s or logic expressions.}

\item{id}{Optional integer ID (internal use).}
}
\value{
An \code{And} expression.
}
\description{
Returns 1 if and only if all arguments equal 1, and 0 otherwise.
For two operands, can also be written with the \code{&} operator: \code{x & y}.
}
\examples{
\dontrun{
x <- Variable(boolean = TRUE)
y <- Variable(boolean = TRUE)
both <- x & y            # operator syntax
both <- And(x, y)        # functional syntax
all3 <- And(x, y, z)     # n-ary
}
}
\seealso{
\code{\link[=Not]{Not()}}, \code{\link[=Or]{Or()}}, \code{\link[=Xor]{Xor()}}, \code{\link[=implies]{implies()}}, \code{\link[=iff]{iff()}}
}
