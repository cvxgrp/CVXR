% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/132_problems_problem.R
\name{problem_unpack_results}
\alias{problem_unpack_results}
\title{Unpack Solver Results into a Problem}
\usage{
problem_unpack_results(problem, solution, chain, inverse_data)
}
\arguments{
\item{problem}{A \code{\link{Problem}} object.}

\item{solution}{The raw solver result from \code{\link{solve_via_data}()}.}

\item{chain}{The \code{SolvingChain} from \code{\link{problem_data}()}.}

\item{inverse_data}{The inverse data list from \code{\link{problem_data}()}.}
}
\value{
The problem object (invisibly), with solution unpacked.
}
\description{
Inverts the reduction chain and unpacks the raw solver solution into
the original problem's variables and constraints. This is step 3 of
the decomposed solve pipeline:
\enumerate{
\item \code{\link{problem_data}()} -- compile the problem
\item \code{\link{solve_via_data}(chain, data)} -- call the solver
\item \code{problem_unpack_results()} -- invert and unpack
}
}
\details{
After calling this function, variable values are available via
\code{\link{value}()} and constraint duals via \code{\link{dual_value}()}.
}
\seealso{
\code{\link{problem_data}}, \code{\link{solve_via_data}}
}
