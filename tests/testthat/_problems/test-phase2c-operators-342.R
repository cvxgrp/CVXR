# Extracted from test-phase2c-operators.R:342

# test -------------------------------------------------------------------------
lhs <- Constant(2)
rhs <- Variable(c(3, 1))
m <- MulExpression(lhs, rhs)
