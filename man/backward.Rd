% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/141_problems_problem.R
\name{backward}
\alias{backward}
\title{Compute the gradient of a solution with respect to Parameters}
\usage{
backward(problem)
}
\arguments{
\item{problem}{A solved \code{Problem}.}
}
\value{
The \code{problem} (for piping); side-effect sets
\code{gradient(param)} on each parameter.
}
\description{
Differentiates through the solution map of \code{problem}: populates
the \code{gradient} slot of each \code{Parameter} with the sensitivity of
a scalar-valued function of the variables (defaulting to the
sum-of-x loss; override per variable by setting
\verb{gradient(variable) <-} before calling) with respect to that
parameter.  Mirrors \code{cvxpy.Problem.backward()}.
}
\details{
Must be called after \code{\link[=psolve]{psolve()}} with \code{requires_grad = TRUE}.
}
\seealso{
\code{\link[=derivative]{derivative()}}, \code{\link[=psolve]{psolve()}}, \code{\link[=gradient]{gradient()}}
}
