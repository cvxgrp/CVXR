# Extracted from test-phase7b-power-tools.R:582

# test -------------------------------------------------------------------------
t_var <- Variable(c(3L, 1L))
x <- Variable(c(3L, 1L))
y <- Variable(c(3L, 1L))
constr <- gm(t_var, x, y)
