# Check if a quad_form DPP Scope is Active

Check if a quad_form DPP Scope is Active

## Usage

``` r
quad_form_dpp_scope_active()
```

## Value

Logical; `TRUE` if a `with_quad_form_dpp_scope` block is active.
