# Extracted from test-phase2c-operators.R:279

# test -------------------------------------------------------------------------
x <- Variable(c(3, 2))
y <- Variable(c(3, 2))
a <- AddExpression(list(x, y))
