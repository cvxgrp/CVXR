% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/005_zzz_R_specific_generics.R
\name{get_bounds}
\alias{get_bounds}
\title{Lower/Upper Bounds of a Leaf}
\usage{
get_bounds(x, ...)
}
\arguments{
\item{x}{An expression (a \link{Variable}/leaf, or a composite expression whose
bounds are propagated from its arguments).}

\item{...}{Passed to methods; currently unused.}
}
\value{
A list \code{list(lower, upper)} of two real matrices matching the
expression shape (column-major). For leaves these come from explicit bounds
and sign attributes; for atoms they are propagated from argument bounds via
interval arithmetic (see \code{bounds_from_args}).
}
\description{
Returns the effective \verb{(lower, upper)} bounds of a leaf, combining its
\code{bounds} attribute with sign (\code{nonneg}/\code{pos}/\code{nonpos}/\code{neg}) and \code{boolean}
attributes. Used by the NLP (DNLP) solve path to form variable bounds.
}
