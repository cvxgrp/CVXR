# Extracted from test-phase2c-operators.R:272

# test -------------------------------------------------------------------------
x <- Variable(c(3, 3), symmetric = TRUE)
y <- Variable(c(3, 3), symmetric = TRUE)
a <- AddExpression(list(x, y))
