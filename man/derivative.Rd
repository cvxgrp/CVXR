% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/136_problems_problem.R
\name{derivative}
\alias{derivative}
\title{Apply the derivative of the solution map to perturbations}
\usage{
derivative(problem)
}
\arguments{
\item{problem}{A solved \code{Problem}.}
}
\value{
The \code{problem} (for piping); side-effect sets
\code{delta(variable)} on each variable.
}
\description{
Forward-mode counterpart of \code{\link[=backward]{backward()}}: reads \code{delta(param)} for
each parameter, applies the cone-program derivative, and writes
the predicted change in each variable's optimum to \code{delta(var)}.
Mirrors \code{cvxpy.Problem.derivative()}.
}
\details{
Must be called after \code{\link[=psolve]{psolve()}} with \code{requires_grad = TRUE}.
}
\seealso{
\code{\link[=backward]{backward()}}, \code{\link[=psolve]{psolve()}}, \code{\link[=delta]{delta()}}
}
