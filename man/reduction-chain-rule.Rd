% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/143_reductions_reduction.R
\name{reduction-chain-rule}
\alias{reduction-chain-rule}
\alias{var_backward}
\alias{var_forward}
\alias{param_backward}
\alias{param_forward}
\title{Reduction chain-rule hooks (dict-in / dict-out)}
\usage{
var_backward(x, del_vars)

var_forward(x, dvars)

param_backward(x, dparams)

param_forward(x, param_deltas)
}
\arguments{
\item{x}{A \code{Reduction}.}

\item{del_vars}{Named list \verb{var-id -> gradient array} (outer representation).}

\item{dvars}{Named list \verb{var-id -> delta array} (inner representation).}

\item{dparams}{Named list \verb{param-id -> gradient array} (inner representation).}

\item{param_deltas}{Named list \verb{param-id -> delta array} (outer representation).}
}
\value{
For \code{var_backward}: the same map in the inner (reduced) representation.

For \code{var_forward}: the same map in the outer (original) representation.

For \code{param_backward}: the same map in the outer (original) representation.

For \code{param_forward}: the same map in the inner (transformed) representation.
}
\description{
Walk the solving chain during \code{Problem$backward()} / \code{Problem$derivative()}.
Each hook takes and returns a named list keyed by \code{as.character(leaf@id)}
whose values are shaped arrays (the leaf's \code{dim}). The base \code{Reduction}
methods are identity pass-throughs.
}
