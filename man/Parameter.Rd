% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/026_expressions_constants_parameter.R
\name{Parameter}
\alias{Parameter}
\title{Create a Parameter}
\usage{
Parameter(
  shape = c(1L, 1L),
  name = NULL,
  value = NULL,
  id = NULL,
  latex_name = NULL,
  ...
)
}
\arguments{
\item{shape}{Integer vector of length 1 or 2 giving the parameter
dimensions. A scalar \code{n} is interpreted as \code{c(n, 1)}.
Defaults to \code{c(1, 1)} (scalar).}

\item{name}{Optional character string name. If \code{NULL}, an automatic
name \code{"param<id>"} is generated.}

\item{value}{Optional initial numeric value.}

\item{id}{Optional integer ID.}

\item{latex_name}{Optional character string giving a custom LaTeX name for
use in visualizations. For example, \code{"\\\\gamma"}.
If \code{NULL} (default), visualizations auto-generate a LaTeX name.}

\item{...}{Additional attributes: \code{nonneg}, \code{nonpos}, etc.}
}
\value{
A \code{Parameter} object (inherits from \code{Leaf} and
\code{Expression}).
}
\description{
Constructs a parameter whose numeric value can be changed without
re-canonicalizing the problem. Parameters are treated as constants for
DCP purposes but their value can be updated between solves.
}
\examples{
p <- Parameter()
value(p) <- 5
p_vec <- Parameter(3, nonneg = TRUE)
gamma <- Parameter(1, name = "gamma", latex_name = "\\\\gamma")

}
