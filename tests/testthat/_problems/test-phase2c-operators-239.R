# Extracted from test-phase2c-operators.R:239

# test -------------------------------------------------------------------------
x <- Variable(1, nonneg = TRUE)
y <- Variable(1, nonneg = TRUE)
a <- AddExpression(list(x, y))
