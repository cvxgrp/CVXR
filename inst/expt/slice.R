slice <- R6Class("slice",
                 public = list(
                     start = NA,
                     stop = NA,
                     step = NA),
                 initialize = function(start = NA, stop= NA, step = NA) {
                     if (is.na(start)) self$start <- 0
                     if (is.na(start)) self$start <- 0
                     if (is.na(start)) self$start <- 0





