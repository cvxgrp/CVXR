% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/005_zzz_R_specific_generics.R
\name{.grad}
\alias{.grad}
\title{Per-atom Subgradient Hook (private)}
\usage{
.grad(x, values, ...)
}
\arguments{
\item{x}{An atom expression.}

\item{values}{A list of numeric values, one per argument.}

\item{...}{Reserved for future use; method dispatch ignores it.}
}
\value{
A list of sparse Jacobians (one per argument).
}
\description{
R counterpart of CVXPY's \code{Atom._grad(self, values)}. Returns a list
of one Jacobian per argument, shape \verb{(prod(arg.shape), prod(self.shape))}.
Called by the chain-rule walker \code{grad(x)}. The leading dot follows R's
private-name convention (cf. \code{.Machine}, \code{.libPaths}, \verb{.cvxr_*}); the
name maps one-to-one onto CVXPY's \verb{_grad}.
}
\keyword{internal}
