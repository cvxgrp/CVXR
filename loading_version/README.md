## README

I saved this tree because this version loads in R
(`devtools::load_all()`) so has a working collate order in
`DESCRIPTION`. When `copy_r_source` is finally working, this tree can
be blown away.

