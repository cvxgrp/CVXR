# Extracted from test-phase1-expressions.R:611

# test -------------------------------------------------------------------------
x <- Variable(3, boolean = TRUE)
val <- matrix(c(0.3, 0.7, -0.2))
result <- project(x, val)
