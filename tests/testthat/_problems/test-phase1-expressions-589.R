# Extracted from test-phase1-expressions.R:589

# test -------------------------------------------------------------------------
x <- Variable(3, nonpos = TRUE)
val <- matrix(c(-1, 2, -3))
result <- project(x, val)
