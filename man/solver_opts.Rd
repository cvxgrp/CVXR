% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/260_zzz_R_specific_solver_opts.R
\name{solver_opts}
\alias{solver_opts}
\title{Create Solver Options}
\usage{
solver_opts(
  use_quad_obj = TRUE,
  feastol = NULL,
  reltol = NULL,
  abstol = NULL,
  num_iter = NULL,
  ...
)
}
\arguments{
\item{use_quad_obj}{Logical. If \code{TRUE} (default), quadratic
objectives use the QP matrix path. If \code{FALSE}, forces conic
decomposition via \code{quad_form_canon}.}

\item{feastol}{Feasibility tolerance (solver-agnostic). Translated to
solver-native name by internal mapping. \code{NULL} uses solver default.}

\item{reltol}{Relative tolerance. \code{NULL} uses solver default.}

\item{abstol}{Absolute tolerance. \code{NULL} uses solver default.}

\item{num_iter}{Maximum iterations. \code{NULL} uses solver default.}

\item{...}{Solver-specific parameters passed directly to the solver
(e.g., \code{eps_abs}, \code{scip_params}, \code{mosek_params}).}
}
\value{
A named list with class \code{"solver_opts"}.
}
\description{
Constructs a structured list of solver options for use with
\code{\link{psolve}} and \code{\link{problem_data}}. Known parameters
are sorted into named slots; solver-specific parameters are collected
in \code{$solver_specific}.
}
\examples{
solver_opts(feastol = 1e-6)
solver_opts(use_quad_obj = FALSE, eps_abs = 1e-7)
solver_opts(scip_params = list("limits/time" = 10))
}
