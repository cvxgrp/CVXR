% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/031_expressions_constants_callback_param.R
\name{CallbackParam}
\alias{CallbackParam}
\title{Callback Parameter}
\usage{
CallbackParam(
  callback,
  shape = c(1L, 1L),
  name = NULL,
  id = NULL,
  latex_name = NULL,
  ...
)
}
\arguments{
\item{callback}{A function (no arguments) returning the parameter's
numeric value.  Re-evaluated on every read of \code{value(param)}.}

\item{shape}{Integer vector of length 1 or 2 giving parameter
dimensions.  Defaults to \code{c(1, 1)} (scalar).}

\item{name}{Optional character string name.}

\item{id}{Optional integer ID. If \code{NULL}, a unique ID is generated.}

\item{latex_name}{Optional LaTeX name for visualisation.}

\item{...}{Other Parameter attributes (e.g., \code{nonneg}, \code{nonpos}).}
}
\value{
A \code{CallbackParam} object (subclass of \code{Parameter}).
}
\description{
A \code{Parameter} whose value is computed by a user-supplied callback
function rather than stored explicitly.  Mirrors CVXPY's
\code{cp.CallbackParam} (\code{cvxpy/expressions/constants/callback_param.py}).
}
\details{
Each call to \code{value(param)} re-evaluates the callback and validates
the returned numeric against the parameter's shape and attribute
domain.  Setting via \code{value(param) <- v} is not allowed and signals
an error.
}
\section{DPP use}{

If \code{p} and \code{q} are scalar \code{Parameter}s, the expression \code{p * q} is
not DPP.  Wrapping it in a \code{CallbackParam},
\preformatted{
pq <- CallbackParam(callback = function() value(p) * value(q))
}
yields a DPP-compliant Parameter whose value tracks \code{p} and \code{q}
automatically.
}

\examples{
p <- Parameter(); value(p) <- 2
q <- Parameter(); value(q) <- 3
pq <- CallbackParam(callback = function() value(p) * value(q))
value(pq)   # evaluates the callback => 6

}
