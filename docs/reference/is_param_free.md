# Check if Expression is Parameter-Free

Returns `TRUE` if the expression contains no parameters.

## Usage

``` r
is_param_free(expr)
```

## Arguments

- expr:

  A CVXR expression.

## Value

Logical scalar.
