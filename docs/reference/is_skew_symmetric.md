# Check if Expression is Skew-Symmetric

Tests whether `X + t(X) == 0` (real matrices only). CVXPY SOURCE:
expression.py line 470-473

## Usage

``` r
is_skew_symmetric(x, ...)
```

## Arguments

- x:

  An expression object.

- ...:

  Not used.

## Value

Logical scalar.
