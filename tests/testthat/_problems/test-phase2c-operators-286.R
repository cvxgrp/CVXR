# Extracted from test-phase2c-operators.R:286

# test -------------------------------------------------------------------------
x <- Variable(1, name = "x")
y <- Variable(1, name = "y")
a <- AddExpression(list(x, y))
