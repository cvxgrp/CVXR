% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/142_problems_param_prob.R
\name{apply_param_jac}
\alias{apply_param_jac}
\title{Adjoint of the parameter -> (c, d, A, b) tensor map}
\usage{
apply_param_jac(param_prog, delc, delA, delb, active_params = NULL)
}
\arguments{
\item{param_prog}{A \code{ParamConeProg}.}

\item{delc}{Numeric vector of length \code{x_length}.}

\item{delA}{Sparse matrix of shape \verb{(m, x_length)} (same shape as
the conic constraint matrix \code{A}).}

\item{delb}{Numeric vector of length \code{m}.}

\item{active_params}{Optional character vector of parameter ids to
restrict the output to.  Default: all parameters.}
}
\value{
A named list mapping \code{as.character(param_id)} to a numeric
array of the parameter's shape.
}
\description{
Given derivatives \verb{(delc, delA, delb)} of a downstream objective
with respect to the conic problem data, returns derivatives with
respect to each \code{Parameter} (keyed by parameter id).  Mirrors
\code{ParamConeProg.apply_param_jac} in \code{cone_matrix_stuffing.py:242-280}.
}
\details{
Reusing the tensor identity that \code{apply_parameters} exploits in
the forward direction:
\code{c = c_tensor[1:x_length, ] \%*\% p}  (omitting the offset row),
\verb{vec(A) // b = A_tensor \%*\% p}     (column-major; b is last block),
the adjoint is just the transpose of the same tensors applied to
the stacked (delc, vec(delA), delb).
}
\keyword{internal}
