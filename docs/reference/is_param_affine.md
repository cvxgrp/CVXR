# Check if Expression is Parameter-Affine

Returns `TRUE` if the expression is affine in parameters and contains no
decision variables.

## Usage

``` r
is_param_affine(expr)
```

## Arguments

- expr:

  A CVXR expression.

## Value

Logical scalar.
