# Extracted from test-phase2h-index-transpose.R:215

# test -------------------------------------------------------------------------
x <- Variable(c(3, 4), name = "x")
tx <- t(x)
nm <- expr_name(tx)
