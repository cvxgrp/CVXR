# Extracted from test-phase2c-operators.R:617

# test -------------------------------------------------------------------------
x <- Variable(1, imag = TRUE)
y <- Variable(1, imag = TRUE)
m <- Multiply(x, y)
