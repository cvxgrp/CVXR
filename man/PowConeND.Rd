% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/128_constraints_power.R
\name{PowConeND}
\alias{PowConeND}
\title{Create an N-Dimensional Power Cone Constraint}
\usage{
PowConeND(W, z, alpha, axis = 2L, constr_id = NULL)
}
\arguments{
\item{W}{A CVXR expression (vector or matrix).}

\item{z}{A CVXR expression (scalar or vector).}

\item{alpha}{A CVXR expression with positive entries summing to 1
along the specified axis.}

\item{axis}{Integer, 2 (default, column-wise) or 1 (row-wise).}

\item{constr_id}{Optional integer constraint ID.}
}
\value{
A \code{PowConeND} constraint object.
}
\description{
Constrains \eqn{(W, z)} to lie in the N-dimensional power cone:
\deqn{\prod W_i^{\alpha_i} \ge |z|, \quad W \ge 0}
where \eqn{\alpha_i > 0} and \eqn{\sum \alpha_i = 1}.
}
\section{Known limitations}{

The R \pkg{clarabel} solver does not currently support the PowConeND cone
specification. Problems involving PowConeND (e.g., exact geometric mean
with more than 2 arguments) should use SCS or MOSEK as the solver, or
use approximation-based atoms (e.g., \code{geo_mean(x, approx = TRUE)}).
}

\seealso{
\code{\link{PowCone3D}}
}
