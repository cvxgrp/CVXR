% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/272_zzz_R_specific_grad_delta_attrs.R
\name{delta}
\alias{delta}
\alias{delta<-}
\title{Access the perturbation \code{delta} of a Variable or Parameter}
\usage{
delta(x)

delta(x) <- value
}
\arguments{
\item{x}{A \code{Variable} or \code{Parameter}.}

\item{value}{A numeric array of the same shape as \code{x}, or \code{NULL}.}
}
\value{
The perturbation (numeric array) or \code{NULL}.
}
\description{
Used by \code{\link[=psolve]{psolve()}} with \code{requires_grad = TRUE} and
\code{Problem$derivative()}.  On a \code{Parameter}, the user sets \code{delta}
to a perturbation of the parameter's value; \code{derivative()} then
reports the predicted change in each \code{Variable}'s optimal value
as \code{delta(variable)}.
}
