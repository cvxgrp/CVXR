# Extracted from test-phase2c-operators.R:196

# test -------------------------------------------------------------------------
x <- Variable(c(3, 1))
y <- Variable(c(3, 1))
a <- AddExpression(list(x, y))
