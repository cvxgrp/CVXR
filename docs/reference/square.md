# Square of an expression: x^2

Square of an expression: x^2

## Usage

``` r
square(x)
```

## Arguments

- x:

  An Expression

## Value

A `PowerApprox` atom with p=2, the same atom `power(x, 2)` builds
