# Extracted from test-phase7b-power-tools.R:616

# test -------------------------------------------------------------------------
t_var <- Variable(c(1L, 1L))
x1 <- Variable(c(1L, 1L))
p <- as.bigq(1L)
